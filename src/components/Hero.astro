<section class="hero">
  <div class="hero-inner">
    <p class="eyebrow">PZ Clean Medical Training</p>

    <h1>
      We Prepare People to Respond Confidently When Emergencies Happen.
    </h1>

    <p class="hero-copy">
      Practical, evidence-based medical training for businesses, response
      teams, healthcare professionals, schools, and outdoor organizations.
    </p>

    <div class="hero-actions">
      <a class="primary-button" href="#training">
        Explore Training Options
      </a>

      <a
        class="secondary-button"
        href="mailto:frank@pzcleanmed.com?subject=Training%20Inquiry"
      >
        Schedule Training
      </a>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 620px;
    display: flex;
    align-items: center;
    color: #ffffff;
    background:
      linear-gradient(
        90deg,
        rgba(5, 23, 37, 0.96) 0%,
        rgba(5, 23, 37, 0.78) 45%,
        rgba(5, 23, 37, 0.25) 100%
      ),
      #0a2236;
  }

  .hero-inner {
    width: 100%;
    max-width: 1200px;
    margin: 0 auto 0 0;
    padding: 100px 24px;
  }

  .eyebrow {
    margin: 0 0 18px;
    color: #d7e2e9;
    font-size: 0.85rem;
    font-weight: 800;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  h1 {
    max-width: 760px;
    margin: 0;
    font-size: clamp(3rem, 6vw, 5.4rem);
    line-height: 1.02;
    letter-spacing: -0.04em;
  }

  .hero-copy {
    max-width: 660px;
    margin: 28px 0 0;
    color: #eef3f6;
    font-size: 1.2rem;
    line-height: 1.7;
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 14px;
    margin-top: 34px;
  }

  .primary-button,
  .secondary-button {
    display: inline-block;
    border-radius: 6px;
    padding: 14px 22px;
    font-weight: 800;
    text-decoration: none;
  }

  .primary-button {
    background: #e97716;
    color: #ffffff;
    transition: all 0.25s ease;
  }

  .primary-button:hover {
    background: #c85f0d;
    transform: translateY(-6px) scale(1.08);
  }

  .secondary-button {
    border: 1px solid rgba(255, 255, 255, 0.75);
    color: #ffffff;
  }

  @media (max-width: 850px) {
    .hero {
      min-height: auto;
    }

    .hero-inner {
      padding: 80px 24px;
    }
  }
</style>
